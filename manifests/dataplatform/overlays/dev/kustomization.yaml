apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dataplatform

bases:
  - ../../base

# Dev uses single-replica Kafka
patches:
  - target:
      kind: StatefulSet
      name: kafka
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
