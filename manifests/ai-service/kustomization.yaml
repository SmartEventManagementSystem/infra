apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ai-service

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - deployment.yaml

images:
  - name: ghcr.io/ems-project/ai-service
    newTag: v1.0.0

commonLabels:
  app.kubernetes.io/part-of: ems-platform
  app.kubernetes.io/managed-by: argocd
